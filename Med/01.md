### You are given two non-empty linked lists representing two non-negative integers. The digits are stored in reverse order, and each of their nodes contains a single digit. Add the two numbers and return the sum as a linked list.

### You may assume the two numbers do not contain any leading zero, except the number 0 itself.

> Example 1 :
```
Input: l1 = [2,4,3], l2 = [5,6,4]
Output: [7,0,8]
Explanation: 342 + 465 = 807.

```

## Explanation:
```
Linked list stores digitis in revese order so 2,4,3 will be 3,4,2 and 5,6,4 will be 4,6,5 whichh be 8,0,7 after addition and the result will be 7,0,8
```
> Why is the reverse order actually useful?

Normally, when adding numbers, we start from the right side.

For example:

But a linked list normally starts from the left/head.

The problem solves this by storing the digits backwards:
